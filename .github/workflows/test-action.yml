name: test-action
on: [push, pull_request]
jobs:
  test:
    strategy:
      matrix:
        python-version: [3.6, 3.7, 3.8]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"
      - name: Python location
        run: echo "Python location ${{ env.pythonLocation }}"
      - name: What is at python location
        run: ls ${{ env.pythonLocation }}"
      #- name: Cache pip
      #  uses: actions/cache@v2
      #  with:
      #    path: ~/.cache/pip # Ubuntu specific  
          # Do not need to cache the dev requirements as they are small
      #    key: ${{ runner.os }}-${{ matrix.python-version }}-pip-${{ hashFiles('**/requirements.txt') }}
      #- name: Install dependencies
      #  run: |
      #  python -m pip install --upgrade pip
      #  pip install -r requirements.txt
      #  pip install -r dev-requirements.txt
      #- name: Code format
      #  run: |
      #  black --check-code --line-length 80 .
      #  flake8 .
      #  mypy
      #- name: Pytest and code coverage
      #  run: python -m pytest --cov=stanza_batch --cov-report term-missing